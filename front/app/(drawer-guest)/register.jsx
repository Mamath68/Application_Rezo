import {CustomText, CustomView} from "../../components";
import {HomeScreenStyles as styles} from "../../theme";

export default function Register() {

    return (
        <CustomView style={styles.container}>
            <CustomText level="h2">Register</CustomText>
        </CustomView>
    );
};
